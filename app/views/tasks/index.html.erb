
<h1>Tasks</h1>

<ul>
  <% @tasks.each do |task| %>
  <li> <%= task.completed %> <%= link_to task.title, task_path(task) %> </li>
  <% end %>
</ul>

<div>
  <p><%= link_to 'Create', new_task_path %></p>
</div>
